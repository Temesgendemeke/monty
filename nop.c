#include "monty.h"
  
/**
 * nop_h - function that does nothing
 * @stack: stack
 * @len: len
 * Return: Void
 */

void nop_h(stack_t **stack, unsigned int len)
{
        (void)(*stack);
        (void)(len);
}
