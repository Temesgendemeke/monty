// #include "monty.h"


char *tokenize(char *buffer)
{
    // char *token = strtok(buffer, " ");
    
   
    // char cmdtok[100][100];
    // int itr = 0;
    // while(token != NULL)
    // {
    //     strcpy(cmdtok[itr],token);
    //     token = strtok(NULL, " /t #");
    //     itr++;
    // }

    // return cmdtok;
}